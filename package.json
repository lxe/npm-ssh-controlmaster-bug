{
  "name": "npm-ssh-controlmaster",
  "version": "3.0.0",
  "description": "Demonstrates the race condition when downloading multiple packages from the same ssh host when ssh controlmaster is enabled",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "test-repo-one": "git+ssh://git@github.com/lxe/test-repo-one.git#master",
    "test-repo-two": "git+ssh://git@github.com/lxe/test-repo-two.git#master"
  }
}
