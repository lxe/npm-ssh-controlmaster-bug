{
  "name": "npm-ssh-controlmaster",
  "version": "3.0.0",
  "description": "Demonstrates the race condition when downloading multiple packages from the same ssh host when ssh controlmaster is enabled",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-ssh-controlmaster-one": "git://github.com/lxe/npm-ssh-controlmaster-bug.git#v1.0.0",
    "npm-ssh-controlmaster-two": "git://github.com/lxe/npm-ssh-controlmaster-bug.git#v2.0.0"
  }
}
